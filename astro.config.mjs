// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    // site: 'https://jyynso.github.io',
    site: 'https://ecwt.github.io',
    output: 'static'
});
