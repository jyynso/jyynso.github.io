---
interface Props {
  class?: string;
	lineStyle?: string;
}

const { class: className = "", lineStyle = "" } = Astro.props;
---

<div class={`md:w-xl w-sm ${className}`}>
	<div class={`md:w-xl w-sm border-t-2 border-xtraMuted/25 ${lineStyle}`} />
</div>