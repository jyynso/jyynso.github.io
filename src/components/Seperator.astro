---
interface Props {
  class?: string;
	lineStyle?: string;
}

const { class: className = "", lineStyle = "" } = Astro.props;
---

<div class={`w-full ${className}`}>
	<div class={`w-full border-t-2 border-xtraMuted/25 ${lineStyle}`} />
</div>